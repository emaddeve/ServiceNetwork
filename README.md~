# ServiceNetwork
 connects you with your neighbors easy and free to share and trade items and services!
 
 this project was developed based on java ee tech.
 
 Resources
To use the resources, change the values "HOSTNAME", "USERNAME"
etc. by your own login information to a MySql database
  in web.xml (in the application "webapp").
 and replace "USERNAME", "PASSWORD". with your own connection information to a MySql database 
  in hibernate.cfg.xml (for the application "webapp").

The "webapp" requires
  "Mysql-connector", "servicelib", "personlib", "hibernate", "gson", "Connector / J"
    (.jar  to be placed  in lib /).

to create the db just run the script.
    mysqldb.sh run bash
